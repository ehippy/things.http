[project]
name = "things-http"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "things.py",
    "fastapi",
    "uvicorn",
]
